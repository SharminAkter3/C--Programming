// relational operator --> <, >, <=, >=, == , !=
// logical operation --> logical and (&&), logical or (||)

#include <stdio.h>
int main()
{
    int tk;
    scanf("%d", &tk);
    if (tk >= 100)
    {
        printf("Pizza Khabo");
    }
    else
    {
        printf("Pizza Khabo na");
    }
    return 0;
}